
val df = "ff"

val mrd = Map(0 -> "x", 1-> "y", 2 -> "z")

def json(name: String)

//val x:(String, List[String]) = ((0 until 2).foldLeft((df, Seq.empty[String])){
//  case ((baseDf, unmapped), counter) => (baseDf, unmapped)
//}) match {
//  case (df, unmapped) => df.
//}

val x = (1, "ff")


